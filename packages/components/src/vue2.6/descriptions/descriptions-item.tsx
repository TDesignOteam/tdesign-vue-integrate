import { defineComponent } from '@vue/composition-api';
import props from '@intel/components/descriptions/description-item-props';

export default defineComponent({
  name: 'TDescriptionsItem',
  props,
});
